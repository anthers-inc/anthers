/*
 * THIS FILE WAS GENERATED BY "@atproto/lex". DO NOT EDIT.
 */

import { l } from "@atproto/lex";

const $nsid = "org.anthers.project";

type $nsid = typeof $nsid;

export { $nsid };

/** A project — a container a creator groups related works and posts under. It deliberately carries NO list of what is in it: a record naming its own contents has to be rewritten every time a work is added or removed, which broadcasts a new version on every change and makes the record a worse copy of a query. Membership belongs on the other end, as an optional field on the work, which may be added later at no cost. */
type Main = { $type: "org.anthers.project";

  /**
   * The project's name, as its creator wrote it. Plain text, for the same reason every other name in these schemas is: a consumer should be able to render it without a sanitizer.
   */
  "title":string;

  /**
   * Where the project can be reached. Not assumed to be an anthers.org address, because a creator hosting their own catalog points at their own.
   */
  "url":l.UriString;

  /**
   * The creator's own summary of what the project is. Plain text, so that every consumer can render it safely without a sanitizer.
   */
  "description"?:string };

export type { Main };

/** A project — a container a creator groups related works and posts under. It deliberately carries NO list of what is in it: a record naming its own contents has to be rewritten every time a work is added or removed, which broadcasts a new version on every change and makes the record a worse copy of a query. Membership belongs on the other end, as an optional field on the work, which may be added later at no cost. */
const main = /*#__PURE__*/ l.record<"tid", Main>("tid", $nsid, /*#__PURE__*/ l.object({"title":/*#__PURE__*/ l.string({"maxGraphemes":300,"maxLength":3000}),"url":/*#__PURE__*/ l.string({"format":"uri","maxLength":2048}),"description":/*#__PURE__*/ l.optional(/*#__PURE__*/ l.string({"maxGraphemes":3000,"maxLength":30000}))}));

export { main };

const $type = $nsid;

type $type = typeof $type;

export { $type };

export const $isTypeOf = /*#__PURE__*/ main.isTypeOf.bind(main);
export const $build = /*#__PURE__*/ main.build.bind(main);
export const $assert = /*#__PURE__*/ main.assert.bind(main);
export const $check = /*#__PURE__*/ main.check.bind(main);
export const $cast = /*#__PURE__*/ main.cast.bind(main);
export const $ifMatches = /*#__PURE__*/ main.ifMatches.bind(main);
export const $matches = /*#__PURE__*/ main.matches.bind(main);
export const $parse = /*#__PURE__*/ main.parse.bind(main);
export const $safeParse = /*#__PURE__*/ main.safeParse.bind(main);
export const $validate = /*#__PURE__*/ main.validate.bind(main);
export const $safeValidate = /*#__PURE__*/ main.safeValidate.bind(main);
