{
	"name": "@anthers/brand",
	"private": true,
	"version": "0.1.0",
	"license": "Apache-2.0",
	"description": "Anthers brand assets that ship — the official logo, plus recolor-ready icon markup generated from a private icon library. The generated markup is committed, so nothing here needs that library.",
	"type": "module",
	"exports": {
		".": "./src/index.ts",
		"./logo/*": "./logo/*"
	},
	"files": [
		"src",
		"logo"
	],
	"scripts": {
		"build": "bun run scripts/build-icons.ts",
		"typecheck": "tsc --noEmit"
	},
	"devDependencies": {
		"@types/bun": "latest",
		"typescript": "^5.8.0"
	}
}
