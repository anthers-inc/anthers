{
	"name": "@anthers/brand",
	"private": true,
	"version": "0.1.0",
	"license": "Apache-2.0",
	"description": "Anthers brand assets that ship — the official logo, plus recolor-ready icon markup generated from Noun Project icons. The source SVGs and the generated markup are both committed.",
	"type": "module",
	"exports": {
		".": "./src/index.ts",
		"./logo/*": "./logo/*"
	},
	"files": [
		"src",
		"logo"
	],
	"scripts": {
		"build": "bun run scripts/build-icons.ts",
		"typecheck": "tsc --noEmit"
	},
	"devDependencies": {
		"@types/bun": "latest",
		"typescript": "^5.8.0"
	}
}
