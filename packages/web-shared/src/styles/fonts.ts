// SPDX-License-Identifier: AGPL-3.0-or-later
//
// The Meadow web families, loaded site-wide via <link> in each app's index.html.
// Reference by inline `fontFamily` (e.g. the Fraunces display serif over the
// Nunito Sans body). Kept here so the shared decor/economics components and the
// marketing pages stay in lockstep on typography.

export const FONTS = {
	fraunces: '"Fraunces", Georgia, "Times New Roman", serif',
	nunito: '"Nunito Sans", system-ui, -apple-system, sans-serif',
	spectral: '"Spectral", Georgia, serif',
	caveat: '"Caveat", "Segoe Script", cursive',
};
